/*
This is a basic block comment that should be completely ignored
*/

// This is a line comment that should also be ignored

// Alias a 64 bit int to be easier to type
let i64 = int(64)

// Create an array type with 1 integer
let MyArray = array(i64, 6)

let MyStruct = struct { a: i64, b: string }

let main = fn() {
  let my_struct_val = MyStruct[a: 32, b: "hello, world"];
  
  let my_val = MyArray[1, 2, 3, 4, 5, 6];
  let mut i = 0;
  for i < 6 {
    debug(my_val[i]);
    i = i + 1;
  }
}
